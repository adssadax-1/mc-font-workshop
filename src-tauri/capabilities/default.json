{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：核心 API + 文件对话框 + 拖拽",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
